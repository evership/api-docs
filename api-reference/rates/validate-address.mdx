---
title: "Validate Address"
api: "POST https://api.evership.app/v1/validate-address"
description: "Validate a South African address before creating a shipment"
---

Validates an address to ensure it can be used for shipping. Use this before creating shipments to catch address issues early.

## Request Body

<ParamField body="address_1" type="string" required>
  Primary street address (e.g. "14 Highland Close")
</ParamField>

<ParamField body="address_2" type="string">
  Secondary address line (e.g. apartment, unit number)
</ParamField>

<ParamField body="suburb" type="string" required>
  Suburb name
</ParamField>

<ParamField body="city" type="string" required>
  City name
</ParamField>

<ParamField body="province" type="string" required>
  Province name (e.g. "Western Cape", "Gauteng")
</ParamField>

<ParamField body="postal_code" type="string" required>
  Postal code
</ParamField>

<ParamField body="country" type="string" required>
  Country name (e.g. "South Africa")
</ParamField>

<RequestExample>
```bash cURL
curl -X POST https://api.evership.app/v1/validate-address \
  -H "apiKey: YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "address_1": "14 Highland Close",
    "address_2": "",
    "suburb": "Vredehoek",
    "city": "Cape Town",
    "province": "Western Cape",
    "postal_code": "8001",
    "country": "South Africa"
  }'
```

```javascript JavaScript
const response = await fetch('https://api.evership.app/v1/validate-address', {
  method: 'POST',
  headers: {
    'apiKey': 'YOUR_API_KEY',
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({
    address_1: '14 Highland Close',
    address_2: '',
    suburb: 'Vredehoek',
    city: 'Cape Town',
    province: 'Western Cape',
    postal_code: '8001',
    country: 'South Africa'
  })
});
const data = await response.json();
```

```typescript TypeScript
interface ValidateAddressRequest {
  address_1: string;
  address_2?: string;
  suburb: string;
  city: string;
  province: string;
  postal_code: string;
  country: string;
}

interface AddressComponents {
  address_1: string;
  address_2: string | null;
  suburb: string;
  city: string;
  province: string;
  postal_code: string;
  country: string;
}

interface ValidateAddressResponse {
  status: string;
  validation_status: "fully_matched" | "partial_match" | "not_found";
  partial_match: boolean;
  matched_address: {
    formatted_address: string;
    address_components: AddressComponents;
    coordinates: { latitude: number; longitude: number };
  }[];
  partial_match_suggestions: string[];
}

const body: ValidateAddressRequest = {
  address_1: '14 Highland Close',
  address_2: '',
  suburb: 'Vredehoek',
  city: 'Cape Town',
  province: 'Western Cape',
  postal_code: '8001',
  country: 'South Africa'
};

const response = await fetch('https://api.evership.app/v1/validate-address', {
  method: 'POST',
  headers: {
    'apiKey': 'YOUR_API_KEY',
    'Content-Type': 'application/json'
  },
  body: JSON.stringify(body)
});
const data: ValidateAddressResponse = await response.json();
```

```python Python
import requests

response = requests.post(
    'https://api.evership.app/v1/validate-address',
    headers={'apiKey': 'YOUR_API_KEY'},
    json={
        'address_1': '14 Highland Close',
        'address_2': '',
        'suburb': 'Vredehoek',
        'city': 'Cape Town',
        'province': 'Western Cape',
        'postal_code': '8001',
        'country': 'South Africa'
    }
)
data = response.json()
```
</RequestExample>

<ResponseExample>
```json 200
{
  "status": "success",
  "validation_status": "fully_matched",
  "partial_match": false,
  "matched_address": [
    {
      "formatted_address": "14 Highland Cl, Devil's Peak Estate, Cape Town, 8001, South Africa",
      "address_components": {
        "address_1": "14 Highland Close",
        "address_2": null,
        "suburb": "Devil's Peak Estate",
        "city": "Cape Town",
        "province": "Western Cape",
        "postal_code": "8001",
        "country": "South Africa"
      },
      "coordinates": {
        "latitude": -33.9375083,
        "longitude": 18.4331856
      }
    }
  ],
  "partial_match_suggestions": []
}
```
</ResponseExample>
